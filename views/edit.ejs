<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tenta3</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/home">Hem</a>
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="/create">Skapa användare</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/edit">Ändra användare uppgifter</a>
      </li>
        </ul>
      </nav>
      <% for(var i = 0; i < data.length; i++) { %>
        <p><%= data[i].id %></p>
        <p><%= data[i].username %></p>
        <p><%= data[i].password %></p>
        <p><%= data[i].roll %></p>
        <button>
            <a href="./card<%= data[i].id %>">Edit Card</a>
        </button>
        <% }; %>
</body>
</html>